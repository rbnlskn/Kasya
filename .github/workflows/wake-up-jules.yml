name: Wake Up Jules on 'Ready'
on:
  issues:
    types: [labeled]

jobs:
  activate-jules:
    # 1. Trigger ONLY when the 'Ready' label is added
    if: github.event.label.name == 'Ready'
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - name: Activate, Assign, and Cleanup
        uses: actions/github-script@v7
        with:
          script: |
            // 2. Add the OFFICIAL lowercase 'Jules' label
            // The bot listens best to strict lowercase.
            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              labels: ['Jules']
            });

            // 3. Assign YOU (The trigger-er)
            await github.rest.issues.addAssignees({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              assignees: [context.actor]
            });

            // 4. CLEANUP: Remove 'Backlog' label if it exists
            try {
              await github.rest.issues.removeLabel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                name: 'Backlog'
              });
            } catch (e) {
              // Ignore if 'Backlog' wasn't there
            }
